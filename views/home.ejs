<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Book Tracker' %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header>
        <h1>WELCOME TO THE BOOK TRACKER</h1>
        <nav>
            <% if (currentUser) { %>
                <a href="/books">My Books</a>
                <a href="/books/index">Add Book</a>
                <form action="/auth/logout" method="POST">
                    <button type="submit">Logout</button>
                </form>
            <% } else { %>
                <a href="/auth/login">Login</a>
               | <a href="/auth/register">Register</a>
            <% } %>
        </nav>
    </header>

    <main>
        <p>Track your reading progress with my simple book tracker!</p>
        <% if (currentUser) { %>
            <p>Welcome back, <%= currentUser.username %>!</p>
        <% } else { %>
            <p>Please login or register to start tracking your books.</p>
        <% } %>
    </main>
</body>
</html>